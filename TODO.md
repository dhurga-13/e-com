# TODO - Make project responsive

## Step 1 ✅

Inspect global layout + CSS and ensure there is no horizontal overflow; add missing utilities like `no-scrollbar`.

## Step 2 ✅

Fix layout wrapper/header overflow behavior (ensure children can shrink: `min-w-0`, safe flex/grid defaults).

## Step 3 ✅

Header polish:

- Verify sticky spacer height isn’t clipping on mobile.
- Ensure mobile drawer content cannot overflow horizontally.

## Step 4

Navbar polish:

- Ensure horizontal scroll container behaves well at mid-width.

## Step 5

Footer polish (only if needed): make newsletter form stack/wrap on small screens.

## Step 6

Run dev server and manually verify key breakpoints: 320px, 375px, 768px, 1024px, 1440px.
